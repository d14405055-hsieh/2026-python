# AI_USAGE.md

## 我問 AI 的問題（5 條）

1. 請依照 week-05 五題題目，先產生每題可執行的 Python 解法。
2. 每題請再提供一版比較好記憶的簡單版本，並加上繁體中文註解。
3. 請依照手打版程式，幫我寫 `unittest` 測試程式並覆蓋基本與邊界案例。
4. 請幫我跑測試並保留測試輸出記錄（log）。
5. 請確認提交路徑符合 `weeks/week-05/solutions/1114405055/`。

## 我採用的建議與原因

1. 採用「每題拆成獨立資料夾」的建議，因為檔案清楚、便於檢查與提交。
2. 採用「兩版程式（easy + handwritten）」的建議，符合 README 的送出標準。
3. 採用「用 `unittest` 做子程序測試」的建議，能直接驗證輸入輸出格式。
4. 採用「每題都產生獨立 log」的建議，方便助教逐題對照測試結果。

## 我拒絕的建議與原因

1. 拒絕只做單一整合測試檔：因為 README 要求每題都要有測試程式與對應紀錄。
2. 拒絕省略中文註解：因為作業明確要求中文註解，且有助於口頭說明。
3. 拒絕只交 AI 版本不交手打版：因為作業要求要有你手打程式。

## AI 建議不完整，我自行修正的案例

案例：一開始將 `unittest` 的執行輸出直接由 PowerShell 管線寫入 log，導致 `stderr` 被當成錯誤訊息，流程顯示不穩定。

修正：改為用 Python `subprocess.run(..., capture_output=True)` 收集 `stdout + stderr` 後寫入各題 log，並追加 `exit_code`，確保每份紀錄一致且可驗證。
